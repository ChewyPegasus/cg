# 🧪 Лабораторная работа №2

## **Основы обработки и сжатия цифровых изображений**

## **Вариант:**

* **Построение и эквализация гистограммы изображения + линейное контрастирование**
* **Сегментация изображения: обнаружение точек, линий и перепадов яркости**

---

# 🎯 Цель работы

Изучить и реализовать методы предобработки и сегментации изображений, соответствующие варианту работы:

1. **Линейное контрастирование.**
2. **Построение гистограммы и её эквализация (RGB и HSV).**
3. **Обнаружение границ (Canny).**
4. **Обнаружение линий (Probabilistic Hough Transform).**

Дополнительно — разработать веб-интерфейс для обработки изображений.

---

# 📚 Теоретические сведения

## 1. Построение гистограммы изображения

Гистограмма отображает распределение яркостей пикселей.

Используется для:

* анализа контрастности изображения,
* контроля яркости,
* подготовки данных к контрастированию.

## 2. Линейное контрастирование

Цель — растянуть диапазон яркости изображения к `[0; 255]`:

$$
I' = \frac{I - I_{\min}}{I_{\max} - I_{\min}} \cdot 255
$$

Метод увеличивает контраст, особенно у тёмных/тусклых изображений.

## 3. Эквализация гистограммы

### 3.1. В пространстве RGB

Эквализация выполняется **по каждому каналу отдельно**, что повышает контрастность, но может искажать цвета.

### 3.2. Эквализация яркости в HSV

Алгоритм воздействует **только на компоненту V (Value)**.
Преимущество:
— сохраняется оттенок и насыщенность,
— более корректная цветопередача.

## 4. Сегментация изображения

### 4.1. Обнаружение границ — метод Канни

Многошаговый алгоритм:

1. сглаживание;
2. вычисление градиентов;
3. non-max suppression;
4. double threshold;
5. hysteresis tracking.

Выход — карта резких перепадов яркости.

### 4.2. Поиск линий — Probabilistic Hough Transform

Позволяет находить отрезки прямых.
Используется поверх карты границ.

---

# 🧩 Реализуемые методы по варианту

| Метод                       | Реализовано | Соответствие варианту |
| --------------------------- | ----------- | --------------------- |
| Линейное контрастирование   | ✔           | да                    |
| Построение гистограммы      | ✔           | да                    |
| Эквализация гистограммы RGB | ✔           | да                    |
| Эквализация яркости (HSV)   | ✔           | да                    |
| Обнаружение границ (Canny)  | ✔           | сегментация           |
| Обнаружение линий (Hough)   | ✔           | сегментация           |

---

# 🧩 Используемые технологии

| Технология       | Назначение                  |
| ---------------- | --------------------------- |
| **FastAPI**      | веб-сервер                  |
| **OpenCV**       | обработка изображений       |
| **Matplotlib**   | построение гистограмм       |
| **Base64**       | передача изображений в HTML |
| **Jinja2**       | интерфейс                   |
| **Python 3.10+** | backend                     |

---

# ⚙️ Структура проекта

```
project/
│── processor.py          # Логика обработки
│── main.py               # Сервер FastAPI
│── templates/
       └── index.html     # Веб-интерфейс
```

---

# 🧩 Описание реализации

## 1. Класс ImageProcessor

### 1.1. Конвертация изображений (около 2 мс)

* `bytes_to_image`: байты → ndarray
* `image_to_base64`: ndarray → JPEG (Base64)

### 1.2. Гистограммы

Метод `create_histogram`:

* RGB — 3 кривые,
* Grayscale — 1 кривая.

Изображение гистограммы отправляется в браузер как Base64.

### 1.3. Реализованные алгоритмы

| Метод                                     | Описание                            |
| ----------------------------------------- | ----------------------------------- |
| `apply_linear_contrast()`                 | растяжение яркости                  |
| `apply_equalize_rgb()`                    | эквализация каждого канала          |
| `apply_equalize_hsv()`                    | корректная эквализация освещённости |
| `apply_canny(t1, t2)`                     | детектор границ                     |
| `apply_hough_lines(th, min_len, max_gap)` | поиск линий                         |

---

# 🌐 FastAPI-интерфейс

## Маршруты

### `GET /`

HTML-интерфейс для загрузки изображений.

### `POST /process`

Принимает:

* файл,
* выбранный метод,
* параметры алгоритма.

Возвращает:

* исходное изображение + гистограмма,
* обработанное изображение + гистограмма.

---

# 📸 Интерфейс приложения

Пользователь может:

* загрузить изображение,
* выбрать метод,
* управлять параметрами,
* получить визуализацию до/после.

Поддерживаются файлы до **50 МБ**.

---

# 📁 Подборка тестовых изображений

Согласно требованиям лабораторной:

| Тип изображения                  | Зачем использовались                | Пример эффекта                |
| -------------------------------- | ----------------------------------- | ----------------------------- |
| **низкоконтрастные**             | проверка линейного контрастирования | значительное усиление яркости |
| **тёмные и пересвеченные**       | оценка эквализации                  | выравнивание яркости          |
| **изображения с чёткими краями** | тестирование Canny                  | чёткие границы                |
| **архитектура / дороги**         | Hough Transform                     | стабильное обнаружение линий  |

---

# ✔️ Результаты работы

## 1. Линейное контрастирование

* улучшает детализацию тёмных областей;
* расширяет динамический диапазон.

## 2. Эквализация RGB

* резко увеличивает контраст,
* возможны искажения цветов.

## 3. Эквализация HSV

* наиболее естественный визуальный результат.

## 4. Canny

* хорошо выделяет перепады яркости,
* подходит для последующей сегментации.

## 5. Hough Transform

* корректно находит прямые по границам.

---

# 🧮 Оценка по критериям

| Критерий                  | Выполнено                      |
| ------------------------- | ------------------------------ |
| Корректность методов (50) | ✔ выполнено, ≥50               |
| Подбор изображений (20)   | ✔ выполнено                    |
| Пояснения (30)            | ✔ добавлены подробные описания |

---

# 📚 Заключение

В ходе лабораторной работы были изучены и реализованы методы обработки изображений, предусмотренные вариантом:

* линейное контрастирование;
* построение и эквализация гистограмм;
* сегментация изображения (границы и линии).

Разработано веб-приложение, которое позволяет применять данные методы с визуализацией результатов. Работа соответствует всем требованиям задания.