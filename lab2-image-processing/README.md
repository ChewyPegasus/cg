````markdown
# 🔬 Image Processing Lab: FastAPI & OpenCV

## 🚀 Обзор проекта

Это веб-приложение, реализующее ключевые алгоритмы и методы обработки изображений с использованием **Python**, библиотеки **OpenCV** для вычислительной части и фреймворка **FastAPI** для создания интерактивного веб-интерфейса.

Проект выполняет следующие задачи:
1.  **Сегментация изображений:** Обнаружение границ (Canny) и линий (Преобразование Хафа).
2.  **Повышение контраста:** Сравнение методов линейного контрастирования и выравнивания гистограммы (эквализации) в цветовых пространствах **RGB** и **HSV** (только компонент яркости).

## ✨ Технологии

* **Фреймворк:** FastAPI (асинхронный веб-сервер)
* **Обработка изображений:** OpenCV (`cv2`) и NumPy
* **Запуск:** Uvicorn
* **Интерфейс:** Jinja2 (HTML/CSS/JS)

## 🛠️ Установка и запуск

Для запуска проекта вам понадобится установленный Python (версии 3.8+).

### Шаг 1: Клонирование репозитория и переход в папку

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
cd fastapi_cv_lab
````

### Шаг 2: Использование Makefile

Мы используем `Makefile` для упрощения управления зависимостями и запуска.

**2.1. Установка зависимостей**

Эта команда создаст виртуальное окружение (`.venv`) и установит все необходимые библиотеки:

```bash
make install
```

**2.2. Запуск в режиме разработки (Рекомендуется)**

Запускает сервер с автоматической перезагрузкой при изменении кода:

```bash
make dev
```

Сервис будет доступен по адресу: **https://localhost:8000**

**2.3. Запуск в режиме Production**

Запускает сервер без перезагрузки:

```bash
make run
```

-----

## ⚙️ Структура проекта

```text
fastapi_cv_lab/
│
├── main.py           # Главный файл FastAPI, управление маршрутами.
├── processor.py      # Модуль с реализацией всех алгоритмов OpenCV.
├── requirements.txt  # Список Python-зависимостей.
├── Makefile          # Скрипты для управления проектом (установка, запуск, очистка).
└── templates/
    └── index.html    # HTML-интерфейс (фронтенд).
```

## 📝 Реализованные алгоритмы

Проект реализует следующие методы обработки изображений:

### I. Сегментация изображений

| Метод | Параметры | Описание |
| :--- | :--- | :--- |
| **Детектор Края Canny** | `Нижний порог`, `Верхний порог` | Двухпороговый детектор для выделения наиболее значимых границ. |
| **Преобразование Хафа (P)** | `Порог`, `Мин. длина линии`, `Макс. разрыв` | Алгоритм для обнаружения прямых линий на изображении. |

### II. Построение и эквализация гистограммы

| Метод | Цель | Сравнение |
| :--- | :--- | :--- |
| **Линейное контрастирование** | Растягивание диапазона яркости пикселей от текущего min/max до полного диапазона [0, 255]. | Простой и быстрый метод повышения контраста. |
| **Выравнивание (Эквализация) RGB** | Независимое выравнивание гистограммы для каналов R, G и B. | **Минус:** Часто приводит к неестественным, искаженным цветам. |
| **Выравнивание (Эквализация) HSV** | Выравнивание только компонента **Яркости (V)** в цветовом пространстве HSV. | **Плюс:** Сохраняет цветовой тон (оттенок и насыщенность), обеспечивая более естественное повышение контраста. |

-----

## 🧹 Очистка проекта

Если вы хотите удалить виртуальное окружение и кэш Python:

```bash
make clean
```

```
```