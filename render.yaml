services:
  - type: web
    name: cg-lab2
    env: python
    rootDir: lab2-image-processing
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
